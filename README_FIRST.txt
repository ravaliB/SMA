J'ai voulu pusher tout le projet mais au final, je push seulement l'essentiel car c'était trop long.

Y a 2 dossiers : src et JNetworkProject.rs
Il faudra placer ces 2 dossiers à leur endroit respectif dans un nouveau projet, pour cela :

1) créer un projet repast (File >> New >> other >> repast symphony >> repast symphony project) avec comme nom "JNetworkProject" (copier coller le titre)

2) Placer les fichiers sources dans un package nommé "jNetworkProject" (idem copier le nom)

3) Dans le dossier "JNetworkProject.rs" du projet créé, remplacer le fichier "context.xml" existant par celui du dépot git
    OU copier coller les lignes 